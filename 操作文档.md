# MissionPlanner 打开步骤

## 第一步：打开 PowerShell

按 `Win + R`，输入 `powershell`，回车。

## 第二步：进入程序目录

```powershell
cd "H:\Project\免疫算法固定翼编队\nuaa_gcs_install20250313(1)\nuaa_gcs_install\nuaa_gcs_install\bin\Release\net461"
```

## 第三步：启动程序

```powershell
.\MissionPlanner.exe
```

---

> **常见错误**：
> - 不要漏掉 `.\`，写成 `\MissionPlanner.exe` 会报错
> - 不要进入 `bin\Release`（那里面只有 md5sum.exe），必须进入 `bin\Release\net461`
